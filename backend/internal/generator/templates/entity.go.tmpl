package entity

import "time"

type {{.ModuleName}} struct {
	ID        uint      `gorm:"primaryKey;autoIncrement" json:"id"`
	{{- range .Fields}}
	{{.NameGo}} {{.TypeGo}} `gorm:"{{if .Unique}}unique;{{end}}{{.GormType}}" json:"{{.NameJson}}"`
	{{- end}}
	CreatedAt time.Time `json:"created_at"`
	UpdatedAt time.Time `json:"updated_at"`
}
